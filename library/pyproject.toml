# pyproject.toml
#downloading import packages and allowing us to create an environmental
#package throught the terminal and downloading more online packages

[build-system]
requires = ["setuptools>=64.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tic-tac-toe"
version = "1.0.0"
